FROM python:3.9-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir \
    requests \
    beautifulsoup4 \
    pillow

# Copy enrichment script
COPY enrich_metadata.py /app/

# Run enrichment script
CMD ["python", "enrich_metadata.py"]
