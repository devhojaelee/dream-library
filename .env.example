# Next.js Environment Variables
NODE_ENV=production
PORT=3000

# JWT Secret (Generate your own: openssl rand -base64 32)
JWT_SECRET=your-secret-key-here

# Add any other environment variables your app needs
